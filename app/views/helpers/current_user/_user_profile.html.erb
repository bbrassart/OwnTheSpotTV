<div class="col s6 offset-s1">
  <img class="media-object rounded--avatars" src="<%= current_user.logo.url(:medium)%>" alt="Skater's avatar">
</div>
<div class="col s4">
  <h4><%= user.username %></h4>
  <p>You are <%= user.stance %>-footed</p>
  <% if user.videos.any? %>
    <% if user.videos.length == 1 %>
      <p>You already posted <%= (user.videos.length) %> video. Keep on shredding!</p>
    <% else %>
      <p>You already posted <%= (user.videos.length) %> videos. Keep on shredding!</p>
    <% end %>
  <% elsif %>
    <p>Don't be scared of the Mob. <%= link_to "Submit your first clip", new_video_path %> and get rewarded!</p>
  <% end %>
  <p>
    <%= link_to edit_skater_path(user) do %>
      <i class="material-icons">build</i>  Update your profile
    <% end %>
  </p>
  <p>
    <%= link_to user, method: :delete, data: { confirm: "Are you sure you want to delete your account?"} do %>
      <i class="material-icons">delete</i> Wanna quit the Mob?
    <% end %>
  </p>
</div>
